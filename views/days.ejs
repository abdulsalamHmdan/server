<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>اختر يوم رمضان</title>
    <style>
        /* إعدادات المتغيرات والألوان */
        :root {
            --bg-color: #f4f5f7;
            --card-bg: #ffffff;
            --primary-btn: #5c524d;
            --secondary-btn: #ffffff;
            --text-main: #2d3436;
            --text-muted: #636e72;
            --border-color: #edf2f7;
            --hover-bg: #f8f9fa;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: var(--text-main);
            /* الخلفية المتدرجة */
            background:
                radial-gradient(900px 600px at 10% 0%, rgba(155, 217, 228, 0.35), transparent 60%),
                radial-gradient(900px 700px at 95% 15%, rgba(68, 49, 41, 0.14), transparent 55%),
                radial-gradient(1100px 700px at 50% 120%, rgba(0, 0, 0, 0.04), transparent 60%),
                var(--bg-color);
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 10px;
        }

        .header h1 {
            font-size: 1.5rem;
            margin: 0;
            color: var(--text-main);
        }

        /* تنسيق البطاقة الحاوية */
        .card {
            background: var(--card-bg);
            border-radius: 25px;
            padding: 25px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.5);
        }

        /* تنسيق الشبكة */
        .target-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* 3 أعمدة */
            gap: 15px;
        }

        /* تنسيق العنصر (اليوم) */
        .target-item {
            background: #fff;
            border: 1px solid var(--border-color);
            border-radius: 15px;
            padding: 20px 10px;
            text-align: center;
            text-decoration: none;
            color: inherit;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: all 0.2s ease-in-out;
            box-shadow: 0 2px 5px rgba(0,0,0,0.02);
            cursor: pointer;
        }

        .target-item:hover {
            transform: translateY(-5px);
            border-color: var(--primary-btn);
            background-color: var(--hover-bg);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .target-label {
            font-size: 0.8rem;
            color: var(--text-muted);
            margin-bottom: 5px;
        }

        .target-value {
            font-size: 1.4rem;
            font-weight: bold;
            color: var(--text-main);
        }

        /* تحسين شكل الأرقام */
        .target-value {
            font-variant-numeric: tabular-nums;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>اختر يوم رمضان</h1>
        </div>
        
        <div class="card">
            <div class="target-grid">
                <% for(let i=1; i<=30; i++) { %>
                    <a href="/admin/<%= i %>" class="target-item">
                      <span class="target-value"><%= i %></span>
                      <span class="target-label">رمضان</span>
                    </a>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>